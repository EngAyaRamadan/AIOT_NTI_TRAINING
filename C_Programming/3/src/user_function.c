#include <stdio.h>
#include "../inc/user_function.h"

// function to add two number
int sum_TwoNumber(int num1, int num2)
{
    return (num1 + num2);
}
// function to add multiple parameter
int sum_muti_pra(int num1, int num2, int num3, int num4, int num5)
{
    return (num1 + num2 + num3 + num4 + num5);
}
// function to print number
void print(int num)
{
    printf(" the sum is %d \n", num);
}
